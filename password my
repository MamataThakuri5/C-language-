#include<stdio.h>
#include<conio.h>
void main(){
	char ch;
	char password[20];
	int i=0;
	printf("enter the password ");
	while((ch=getch())!=13){
	password[i]=ch;
	i++;
	printf("*");}
	password[i]='\0';
	printf("enter the password is%s ",password);
}
